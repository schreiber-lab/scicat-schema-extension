name: addon_api_env
channels:
  - defaults
  - conda-forge
dependencies:
  - python <= 3.9
  - flask
  - gunicorn
  - apispec
  - flask-apispec
  - cerberus
  - flask-pymongo
  - flask-cors #should only be there for dev.


  #for testing you may want to add
  # pytest
  # pytest-mongodb (only on pip)

  # for code formatting:
  # black
